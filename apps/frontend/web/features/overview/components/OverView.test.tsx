import { ProjectRole } from "./ProjectRole";
import { render, screen } from "@testing-library/react";

describe("./ProjectRole", () => {
  it("should check the OverView Component", () => {
    render(<ProjectRole />);
  });
});
